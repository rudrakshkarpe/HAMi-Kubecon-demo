apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-jozu
  labels:
    app: vllm-jozu
    pipeline: kitops-jozu
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-jozu
  template:
    metadata:
      labels:
        app: vllm-jozu
        pipeline: kitops-jozu
      annotations:
        hami.io/node-scheduler-policy: binpack
        hami.io/gpu-scheduler-policy: binpack
    spec:
      schedulerName: hami-scheduler
      # --- KitOps initContainer: pull ModelKit from Jozu Hub + unpack to /models ---
      initContainers:
        - name: kitops-init
          image: hami-kitunpacker:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: MODELKIT_REF
              value: "jozu.ml/jonathangamer202002/qwen3-4b-instruct:latest"
            - name: UNPACK_PATH
              value: "/models"
            - name: MODEL_SUBDIR
              value: "qwen3"
          volumeMounts:
            - name: modelkit
              mountPath: /models
      containers:
        - name: vllm-jozu
          image: hami-vllm-jozu:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: MODEL_DIR
              value: "/models/qwen3"
            - name: SERVED_NAME
              value: "qwen3-4b-instruct"
            - name: MAX_MODEL_LEN
              value: "8192"
            - name: GPU_MEM_UTIL
              value: "0.85"
          ports:
            - containerPort: 8000
          resources:
            limits:
              nvidia.com/gpu: "1"
              nvidia.com/gpucores: "30"
              nvidia.com/gpumem: "30000"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 40
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 60
          volumeMounts:
            - name: modelkit
              mountPath: /models
            - name: dshm
              mountPath: /dev/shm
      volumes:
        # node-local cache shared with sglang-jozu so the 8 GB ModelKit is
        # pulled from Jozu Hub only once.
        - name: modelkit
          hostPath:
            path: /var/lib/hami-demo/modelkit
            type: DirectoryOrCreate
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 4Gi
---
apiVersion: v1
kind: Service
metadata:
  name: vllm-jozu
spec:
  selector:
    app: vllm-jozu
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      name: vllm-svc
  type: ClusterIP
